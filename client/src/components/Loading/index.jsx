import { StyledContainer, StyledSpinner } from "./style";

const Loading = () => {
  return (
    <StyledContainer>
      <p>Loading...</p>
      <StyledSpinner />
    </StyledContainer>
  );
};

export default Loading;
